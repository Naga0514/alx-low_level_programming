#include <stdio.h>
#include "main.h"

/**
 * main - Entry point.
 * Description: write a program that prints "_putchar" followed by a new line.
 * Return: Always 0 (Success.)
 */
int main(void)
{
	char i = "_putchar";
	while ((c = getchar()) != EOF)
		putcahr("%s\n", i;)

	return (0);
}
